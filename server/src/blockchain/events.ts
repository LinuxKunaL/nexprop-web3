const propertyCreated = (
  tokenId: string,
  owner: string,
  metadataCID: string,
) => {
  console.log(tokenId);
  console.log(owner);
  console.log(metadataCID);
};

export default { propertyCreated };
